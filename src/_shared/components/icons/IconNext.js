import * as React from "react";

const IconNext = (props) => (
  <svg width={13} height={18} xmlns="http://www.w3.org/2000/svg" {...props}>
    <path d="m2 1 8 8-8 8" stroke="#1D2026" strokeWidth={4} fill="none" />
  </svg>
);

export default IconNext;
